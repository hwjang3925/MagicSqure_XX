# Session3 Contract Align — MagicSquare_xx

| 항목 | 내용 |
|------|------|
| 프로젝트 | MagicSquare_xx |
| 단계 | 세션 3 — `validate_lines` 계약 정합 (워크북 리뷰·보완) |
| 일자 | 2026-06-10 |
| 상태 | **종료** |
| Transcript | [Prompting/02.Session3_Contract_Align_Transcript.md](../Prompting/02.Session3_Contract_Align_Transcript.md) |

---

## 요약

`Report/Session3_Workbook.md`의 R-G-I-O·성공 기준 3개를 `validate_lines` 계약(§2 Output + §8 Rule·Command·Test Loop)과 대조 리뷰했다.
구현 코드 없이 문서 계약만 기준으로 6개 빠짐·어긋남 항목을 식별했다.
이어서 워크북 §2·§3·§8을 수정해 3상태(`pass`/`fail`/`incomplete`) 전체의 `ok`·`status`·`lines[]` 매핑과 R5 예외를 일관되게 고정했다.

## 산출물

| 파일 | 설명 |
|------|------|
| `Report/Session3_Workbook.md` | R-G-I-O Output, 성공 기준 3개, §8 Rule·Command·Test Loop 계약 보완 |
| `Report/02.Session3_Contract_Align_Report.md` | 본 보고서 |
| `Prompting/02.Session3_Contract_Align_Transcript.md` | 세션 Transcript |

## 결정·규칙

- **계약 기준:** `validate_lines` 구현 전 문서상 계약 = 워크북 §2 Output + §8 Rule·Command·Test Loop
- **pass:** `ok=true`, `status="pass"`, `lines[]=[]`
- **fail:** `ok=false`, `status="fail"`, `lines[]`에 틀린 줄만 `{ id, sum, expected: 34 }` (0 없을 때 10선 전부 검사)
- **incomplete (R5):** `0` 포함 시 합 계산·34 비교 생략 → `ok=false`, `status="incomplete"`, `lines[]=[]` (`fail`과 구분)
- **성공 기준 #1:** `0` 없는 완성 격자에서만 10선×34 검증; R5는 별도 분기로 명시

## 다음 단계

- `tests/test_validate_lines.py`에 RED 테스트 작성 (`/tdd-red` Command 참조)
- `src/validate_lines.py` GREEN 구현 — 보완된 계약에 맞춰 `ValidationResult` 반환
- (선택) `diag:main` / `diag:anti` 셀 좌표 정의를 계약에 추가
